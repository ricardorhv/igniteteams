import { Text, View } from "react-native";

export function Groups() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Groups</Text>
    </View>
  );
}
